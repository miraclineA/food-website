import { FoodWebsitePipe } from './food-website.pipe';

describe('FoodWebsitePipe', () => {
  it('create an instance', () => {
    const pipe = new FoodWebsitePipe();
    expect(pipe).toBeTruthy();
  });
});
